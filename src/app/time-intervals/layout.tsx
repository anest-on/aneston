const TimeIntervalsLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="flex flex-col justify-center h-full w-screen bg-gray-900 text-gray-200 text-base">
      {children}
    </div>
  )
}

export default TimeIntervalsLayout
