const RegisterLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="flex justify-center h-screen w-screen bg-gray-900 text-gray-200 text-base">
      {children}
    </div>
  )
}

export default RegisterLayout
